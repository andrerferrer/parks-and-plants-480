# README

To run it:

```
    git clone git@github.com:andrerferrer/parks-and-plants-480.git gardens-and-plants
    cd gardens-and-plants
    bundle install
    yarn install
    rails db:create db:migrate db:seed
    rails s
```
